{
  "name": "db-mysql-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:first-sync": "node ./scripts/first-sync-db.js",
    "db:migration:all": "sequelize db:migrate",
    "db:migration:undo": "sequelize db:migrate:undo",
    "db:migration:create": "sequelize-cli migration:generate --name"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.21",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.2",
    "sequelize-auto": "^0.8.1",
    "structure": "^2.0.1",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "sequelize-cli": "^6.2.0"
  }
}
